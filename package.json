{
  "name": "ritual",
  "version": "0.1.0",
  "description": "One-tap reorder agent on Swiggy Builders Club MCP — retry-safe, idempotent, confirmation-gated order placement.",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "auth": "bun run src/auth-cli.ts",
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  },
  "peerDependencies": {
    "typescript": "^5.5.0"
  }
}
